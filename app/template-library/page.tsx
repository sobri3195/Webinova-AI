"use client";
import { templates } from "@/lib/data";
import { useMemo, useState } from "react";
export default function TemplateLibrary(){const [q,setQ]=useState(""); const data = useMemo(()=>templates.filter(t=>t.title.toLowerCase().includes(q.toLowerCase())),[q]); return <div className="space-y-4"><h2 className="text-2xl font-semibold">Template Library</h2><input className="input" placeholder="Cari template..." value={q} onChange={e=>setQ(e.target.value)} /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.map(t=><div key={t.id} className="card p-4"><h3 className="font-semibold">{t.title}</h3><p className="text-xs text-slate-500">{t.category}</p><p className="my-2 text-sm">{t.description}</p><p className="text-xs">{t.tags.join(" • ")}</p><div className="mt-3 flex flex-wrap gap-2"><button className="btn border border-border">Preview</button><button className="btn border border-border">Duplikasi</button><button className="btn bg-primary text-white">Generate Prompt</button></div></div>)}</div></div>}
